discord.py==2.4.0
python-dotenv==1.0.1
PyNaCl==1.5.0
# For Python 3.13 only (stdlib audioop was removed):
audioop-lts==0.2.1; python_version >= "3.13"

# Note: http.server is part of Python stdlib, no additional package needed
